/*function NewRoom(){
    if(collideRectCircle()){
        	
    } else if(){

    }

    for(door of rooms[roomPos.x][roomPos.y].doors) {
        if(door.x != undefined) {
            rectbufferpos = createVector(door.x * ((rooms[roomPos.x][roomPos.y].size.x - doorWidth) / 2), 0)
            if(door.x != 0) {
                rectbuffersize = createVector(doorWidth, doorHeight)
                if(collideRectCircle(rectbufferpos, rectbuffersize, boi.pos, boi.size/2)){

                } 
            }else if(door.y != 0) {
                rectbuffersize = createVector(doorHeight,doorWidth)
                if(collideRectCircle(rectbufferpos, rectbuffersize, boi.pos, boi.size/2)){

                }
            }
        }
    }
}
*/